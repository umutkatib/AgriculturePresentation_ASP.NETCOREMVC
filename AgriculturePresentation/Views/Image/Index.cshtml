@using EntityLayer.Concrete
@model List<Image>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Görsel Listesi</h4>
            <div class="table-responsive">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>
                                #
                            </th>
                            <th>
                                Başlık
                            </th>
                            <th>
                                Düzenle
                            </th>
                            <th>
                                Sil
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach(var item in Model)
                        {
                        <tr>
                                <td class="py-1"><img src="@item.ImageUrl" alt="image" /></td>
                                <td>@item.Title</td>
                                <td><a href="/Image/EditImage/@item.ImageID" class="btn btn-outline-primary">Güncelle</a></td>
                                <td><a href="/Image/DeleteImage/@item.ImageID" class="btn btn-outline-danger">Sil</a></td>
                        </tr>
                        }

                    </tbody>
                </table>
                <a href="/Image/AddImage" class="btn btn-outline-success mt-4">Yeni Görsel Girişi</a>
            </div>
        </div>
    </div>
</div>

